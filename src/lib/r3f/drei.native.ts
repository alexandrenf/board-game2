import React from 'react';

export { OrbitControls, RoundedBox, useGLTF } from '@react-three/drei/native';

type HtmlProps = { children?: React.ReactNode } & Record<string, unknown>;

export const Html: React.FC<HtmlProps> = () => null;
